# Core workflow maintenance

This directory is intentionally empty; add no placeholder module. A future
workflow module may import only `dsl` modules and standard libraries, and its
registry, help, model definitions, and execution fixtures belong in `cli`.
