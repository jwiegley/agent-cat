# Core workflow maintenance

- This package is intentionally empty; add no placeholder module.
- Future workflow modules may import only `agentic-dsl` and standard libraries.
- Keep registry, help, model definitions, and execution fixtures outside this package.
