# Core workflow maintenance

This directory is intentionally empty, so add no placeholder module. A future
workflow module can import only `dsl` modules and standard libraries. Its
registry, help, model definitions, and execution fixtures belong in `cli`.
