# workflow/core

`workflow/core` is reserved for stable, generally useful workflow definitions.
It holds no module at present and is not listed in `agentic.cabal`. A workflow
added here may depend only on `dsl`; registry rows, help pages, model
definitions, execution fixtures, planning, and pricing stay outside.
