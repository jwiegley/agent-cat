# workflow/core

`workflow/core` is reserved for stable workflow definitions of general use. It
holds no module at present, and `agentic.cabal` does not list it. A workflow
that is added here can depend only on `dsl`. Registry rows, help pages, model
definitions, execution fixtures, planning, and pricing stay outside.
