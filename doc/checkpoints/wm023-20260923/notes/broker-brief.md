# Broker-backed first TUI workflow

Current implementation source is broker-source/, branch service-tui-broker.purvEwEv, based on canonical678326bdf8cc36e63700ee33fa93df38142b6d70. The original source/ worktree and pre-broker-work-in-progress.patch remain unchanged and preserved. Parent carried the eight-file service/client proposal onto the accepted broker base without conflict. Canonical remains unchanged by this continuation. Parent is sole writer and executable owner.

The current goal task is first-frontend-broker-workflow. Its acceptance remains actual production TUI interaction through a running protected manager: submit, exact explicit approval, observe, typed Bool False and supported recovery/control, terminal completion and verified result retrieval. No compilation, helper, mocked success or native fallback is that milestone. WM001–WM022/G0/G1 remain closed, WM023 remains partial, and the broker prerequisite is integrated. RabbitMQ and other frontends remain later work.

The returned manager-public-observation-code-mud93a27-9pvquo report is a proposal, not implemented or validated code. Parent confirmed its version-domain mismatch: Worker selects frontend session2, which uses runtime observation3, while the public schema admitted only frontend session1/runtime1–2. The isolated current contract now explicitly adds the existing native session2/runtime3 domains and permits RuntimeSummary.protocolVersion3. Public API/snapshot/event versions and native wire/storage encodings are unchanged. Original fixtures and unknown-version99 refusals remain unchanged. Four additional fixtures cover actual supported versions and immediate unsupported versions. This explicit compatibility-domain amendment is recorded in doc/api/README.md and requires review with the service integration, not silent relabelling or downgrade.

native-version-contract-red ran2026-09-23T01:42:52–01:42:53Z and failed on the expected valid versions-current-native fixture. native-version-contract-fixed failed01:44:41–01:44:42Z because the new fixture definition mistakenly named RunRuntime rather than the existing RuntimeSummary schema. Parent corrected only the fixture schema name. native-version-contract-fixed-second passed01:46:08–01:46:09Z:99schemas,29operations,331payload cases,20SSE cases and3byte-bound downloads. These are contract checks, not service execution.

New production source extends the existing Store owner with withStoreCataloguesWatch, retaining one reader charge and the original configuration/watch lifetime. Authorization.withAuthorizedCatalogues observes filtered grants and view facts together, with later revalidation under that same loan. State now exposes an opaque ProjectionCut, captureProjectionCut and restoreProjectionCut so public bindings can be captured in the same read transaction as the immutable native prefix, while replay remains outside SQL. Existing profile views and restoration entry points retain their owners and behavior. No SQL/admission budget was widened and no opaque operation is replayed.

broker-service-build-first passed Werror compilation at02:10:08–02:10:45Z. Manager.Observation now projects complete public snapshot fields and items from one captured native prefix plus public bindings, without exposing private references. public-observation-build-first passed02:28:45–02:29:45Z. The existing ArtifactCheck and artifact_contract.py gained an observation case. public-observation-native-first passed02:55:45–02:56:17Z at N1 then N8, checking actual catalogue grants, one reader/configuration loan, escaped-view refusal, immutable-prefix bindings across a subsequent terminal ingestion, exact terminal bills/result metadata, truthful native protocol3, current scope removal, public JSON schemas and the existing composition assertions. These are real owner/contract checks, not HTTP/service/TUI acceptance.

The bounded read-only page-cursor-contract report from scout496b7961-e721-443f-85b0-81154563777e resolved two remaining implementation choices. A deterministic public alias of the durable stream, authority epoch and acknowledged stable view is conforming. Process generation must remain a separate local fence and must not alter that public alias on equivalent restart. Completed single-page/final-page response retirement is unspecified by the frozen contract. The selected policy releases terminal sets only after outstanding sends unwind, and lost terminal replies require resnapshot. Earlier pages remain immutable until expiry or invalidation. Document these as implementation choices, not pre-existing frozen rules.

Authorization distinguishes process generation, execution-profile revisions and durable permission facts. Page bindings include execution-profile revisions. Cursor bindings include current credential/client authority, grants, deadlines and configured profile membership, but not fresh installation tokens. Neither fingerprint is authority. Pages reserves client/global capacity before materialization and retains charges through active sends. Its native lifecycle checks remain pending.

Events, Application, owner-scoped reads and foreground Manager/CLI composition now exist in the isolated source. Actual authenticated HTTPS capabilities, profiles, polling and SSE have executed locally. Overview, public workflow DTOs and TUI service mode remain incomplete. The HTTP proposal was adapted at the original owners. Delayed snapshot/projection materializers revalidate before starting work and afterward. Artifact response callbacks now return a polymorphic result, allowing WAI to finish the actual send within the original file and authorization loans.

http-application-build-first ran05:07:58–05:08:47Z and failed after library compilation because generalized download callbacks left nine negative test actions with ambiguous result types, producing ten diagnostics. Parent missed that failure in the displayed build output and wrongly continued with older executables. http-scoped-native-first and http-native-first therefore do not validate the changed source. The first HTTP fixture refused credential administration with storage-unavailable before writing a secret. Its root and all logs remain intact. A read-only listing gave the same refusal. A shell-quoted diagnostic failed syntactically, and a first GHC expression lacked the Text instance import. The corrected expression showed that the newly built library loads this configuration, while the old agentic-run executable had not been rebuilt. These diagnostics did not replay issuance. Explicit IO () annotations fixed the test inference. Every later executable sequence checks and displays exit metadata.

http-application-build-second passed05:20:23–05:20:37Z. http-native-corrected passed the current N1 observation/composition checks, reached real TLS and a schema-valid ProfilePage, then failed05:20:58–05:21:02Z because the test tried to re-encode the contract parser's Decimal values. The test now preserves exact response bytes. http-native-second passed05:22:02–05:22:06Z at N1 then N8. Its root is /Users/johnw/Products/k.M0a5ItPm/tmp/http.lLSFMrrJ, resolved from the exact run interval because that copied command omitted the root print. Both profile facts were ready. All four original foreground processes were joined with exit -2 after SIGTERM was translated to UserInterrupt. Native checks exercised local TLS1.3 with certificate verification, authentication before resource lookup, host/origin/cookie/forwarded refusals, live credential revocation, and same-root restart.

events-restart-red failed05:27:11–05:27:25Z on the genuine assertion that ordinary restart preserves the public stream alias. Profile.reloadProfiles deliberately creates a fresh execution revision on every installation. Authorization now retains that revision in local response/page facts while separating durable permission facts for cursors. events-restart-fixed passed05:29:07–05:29:52Z at N1 then N8, including filtered-prefix advancement, retained visible tail, escaped-view refusal, stable restart alias with changed page binding, future/noncanonical cursor refusal and permission-change invalidation. The public event cursor remains independent of native Runtime sequences.

events-floor-red and sse-native-second exposed a faulty new fixture assertion as well as the read boundary under construction. The timestamp mutation helper appends a fresh invalidation, so oldestCursor must precede that fresh record, not equal high-water. That original assertion is not valid red evidence for the production fix. The corrected test captures the pre-aging boundary, requires it to be the advertised floor while physical deletion remains behind it, and verifies that the fresh tail is retained. Store now obtains one cutoff and the last expired sequence within the existing transaction. Deletion remains limited to256 rows, batch reads to64, and SQL budgets are unchanged. Controlled timestamps are selection fixtures, not elapsed-aging evidence.

SSE uses the original Store AuthorizationWatch as its charged reader lifetime. Per-batch configuration/watch loans borrow that original charge, expire before releasing configuration, and end before waits. Each pump is single-use, with two streams per client, bounded batches, five-second callback writes and fifteen-second heartbeat comments. Public SSE blocks and polling batches share the same retained reader and cursor filtering. The public version-domain amendment also admits the already supported manager schemas1–12, with a new schema13 refusal. No SQL/native format or dependency version changed.

sse-native-first failed05:47:54–05:48:20Z because Scope has no ToJSON instance. Rendering with the existing scopeName fixed it. sse-native-second compiled and passed the expanded contract, then stopped on the faulty floor fixture. sse-native-third passed05:51:12–05:51:25Z at N1 then N8, including actual matching polling/SSE blocks, two-reader admission and third-reader refusal, live revocation closing both streams, original process joins and same-root restart. Contract evidence is now99schemas,29operations,333payload cases,20SSE cases and3byte-bound downloads.

response-reader-busy-red failed05:54:19–05:54:25Z on another thread's reader admission while a response held configuration. That contention was incorrectly classified StoreUnavailable. The original Store owner now reports StoreBusy only for the explicit SupervisionUnavailable guard refusal, preserving fail-fast admission and all other failures. This lets Service's existing bounded retained-head ingestion retry classify contention correctly, without adding replay or waits. response-reader-busy-fixed passed05:55:42–05:56:39Z after Werror builds at N1 then N8 for observation, composition, event selection and actual HTTPS/SSE. Current evidence root is /Users/johnw/Products/k.M0a5ItPm/tmp/http.2Kg6Y29L. No real workflow has yet been submitted through HTTP or the TUI.

The public workflow catalogue and Overview routes now exist. Catalogue discovery retains bounded actual help through the original query owner, renders public decimal counts and InputDeclaration fields, and omits private invocation/control-FD fields. Workflow IDs still use the established profile/name identity. The help group has one query deadline and the complete projection uses the existing discovery byte ceiling. /workflows requires profileId, page tokens bind that query, and individual Workflow GETs use the same retained projection.

catalogue-build-first passed06:23:18–06:24:11Z. catalogue-native-first ran06:25:29–06:26:01Z, passing the full local profiles/root-separation gate at N1/N8 before Configuration N1 stopped on an old assertion expecting only the capability and catalogue queries. The new help query is now included in both exact-argv and gated-reload assertions. catalogue-native-corrected passed06:27:25–06:27:44Z, including the full configuration gate N1/N8 and real HTTPS workflow collection/item reads, schema checks, profile filtering and existing transport/SSE checks. Its roots are credentials.QHYL3FHM/build/manager-configuration.IzMYE9 and /Users/johnw/Products/k.M0a5ItPm/tmp/catalogue.052NCJLW.

Configuration, Store and Authorization now expose catalogue plus permitted invocation observations under the same original response loan. Legacy catalogue callbacks delegate and retain their existing interfaces. History's new managedRunInView uses the original file owner and ingested Runtime prefix while reusing the existing Run renderer with borrowed invocation observations. State decisions likewise borrow the existing response view, and Approval's transactional preparation projection is shared. These are observational APIs, never worker reconstruction.

Overview reserves through the existing Pages producer boundary, captures all selected resource IDs with its authorized cursor/floor, materializes through those owners outside SQL, and refuses if the durable commit boundary changes. Its five-second producer budget, 64MiB encoded collection bound and unchanged SQL budgets remain explicit. It does not replay a failed or changed collection. The actual /snapshot route now exists, so Problem snapshot links were restored. /runs/{id} also has an owner-scoped renderer. OutputPage now includes its required runId.

overview-build-first passed07:00:28–07:01:31Z. overview-http-first passed07:05:05–07:05:15Z at N1 then N8 on /Users/johnw/Products/k.M0a5ItPm/tmp/overview.f6z1NppT. The real protected service served an empty Overview, accepted a single draft-creation POST, served the complete matching request in the next Overview, and delivered its actual request/command invalidations through polling and SSE. At that point no workflow execution had started through HTTP. Later execution evidence follows below. Concurrent-cut, native page lifecycle, slow-reader/runtime coexistence and broader negative checks remain incomplete. No application integration has occurred.

The complete protected HTTP mixed-controls journey now passed at N1 then N8 in http-mixed-ordered-loans,09:17:56–09:19:14Z, exit0. Current root is /Users/johnw/Products/k.M0a5ItPm/tmp/mixed-http.n7QrnvFN. It used RoutingFixedPointProbe and the deterministic ACP retry adapter, real exact approval, typed Bool False, offered retry, native terminal state, verified result bytes and cleanup-governed admission release. Both runs also exercised polling/SSE, quotas, revocation, original joins and same-root restart. This is not the required actual TUI interaction. All prior failures and roots remain preserved in mixed-http-validation.md.

The independent liveness review (workflow28b08d83-a725-4ddd-938d-8a9e061cbe85, childa88e6ad4-c916-4c04-9f33-ef5ef06e9f86) returned BLOCK for remaining file/configuration order inversions, not for the bounded unentered-callback deferrals. Parent corrected Overview, run and decision owners to acquire files first and borrow that one root through their materializers. response-order-second passed09:16:29–09:16:59Z at N1/N8, including ordering barriers, original loan retention and escaped materializer refusal. response-order-first's missing concatenation operators remain recorded. The corrected delta has not received continued review clearance.

The review also proved a separate slow-response ceiling: ingestion's five-second StoreBusy allowance can end while a response retains configuration over multiple five-second chunks. This is not fixed or proved harmless. Remaining focused negative checks and a policy-consistent disposition are required before integration. Entered fail-fast refusal remains a legitimate preparation failure, not authority for automatic re-enqueue. Latest failures do not prove their exact original cause.

Public Client profile loading, strict verified-TLS configuration, complete bounded page assembly, version/limit validation, exact-URI observations, Last-Event-ID polling,22-character nonce encoding, post-body credential checks and bounded response headers are now implemented in Client.hs. Direct dependencies only expose already pinned crypton packages, without version/lock/compiler changes. ClientCheck.hs and client_native.py exercise the public facade rather than server internals. client-native-first passed twelve boundary cases N1 then N8 at09:48:25–09:48:40Z. That is an external HTTPS fixture, not workflow or TUI acceptance.

Service-client enablement is now blocked on explicit dependency authorization. client-manager-native-first failed09:50:49–09:51:08Z at N1 with TransportUnavailable. client-ip-san-diagnostic failed09:54:49–09:54:50Z at N1, isolating a positive trusted IP-SAN certificate case. Its positive assertion stays intact. Separate official HSEC-2026-0008 and CERT/CC VU#862559 research confirms CVE-2026-9648 affects the pinned crypton-x509-validation1.6.14 and crypton-x5091.7.7. Fixes are identified in1.9.1. No hostname bypass, permissive hook or dependency update was applied. Detailed sources, original roots, build failures and next criteria are in client-security-blocker.md.

Issue acat-tls-name-constraints-0h7q and WM023 comment302 record the blocker. The authority tracker was flushed with230issues,324dependencies,149labels and302comments. Canonical shared records match after only the established Org escape normalization, and the four authority-only records remain preserved. Canonical documentation passed10:06:40–10:06:43Z. Tracking-only commit8f6bbc4cb4f7ca5bdc7d9d2fd4d06e265f246bdc updates PLAN and the handoff. Canonical application code is still the reviewed678326b baseline. The isolated service/client source remains uncommitted and unreviewed, and the actual TUI path remains unimplemented.

Read-only TUI proposal workflow b1b42af1-2b69-438d-9bd8-0ca52487cdfb launched child33e84949-914d-4bc6-9b9e-985380e41f15, key tui-service-code. The child completed the 1343-line tui-service-code.md proposal after two supervisor clarifications. The orchestration reported failure because its JavaScript continuation was not persisted, and the child is not resumable. The report exists and is proposal evidence only, not compilation, source edits or PTY execution. Do not relaunch it merely to obtain the same proposal. Parent clarified that frozen OpenAPI DTOs are authoritative and approved an explicit absolute client-profile file for --tui --service CLIENT_PROFILE: closed JSON {version:1,endpoint,credentialFile,caFile}, with HTTPS /v1 and absolute client-side paths, no implicit name registry and no manager-store access.

The TUI proposal remains useful but requires two semantic corrections beyond its deprecated closeManager call and unsupported nix develop commands. Exact review hashes native prompt-transport bytes, which add the declared LF, while logical literal text remains unchanged. Approval receipts intentionally remain dispatch-attempted even after runtime completion, as canonical tests require. The UI must follow independently observed run evidence without falsely clearing that pending delivery state or waiting forever before allowing valid independently authorized controls.

The isolated source now implements the real read-only TUI service entry, --tui --service ABS_CLIENT_PROFILE. New tui/src/Agentic/Tui/Service.hs strictly decodes public Profile and Workflow DTOs and loads complete client page sets. It retains exact public identities, nullable fields, false values, arbitrary-precision decimal counts and declared input order. Pure display adapters do not manufacture a native invocation or execution capability. New ServiceTests.hs covers refusal of unknown/private fields, missing nullable fields, numeric/noncanonical counts, duplicate inputs/properties, and metadata preservation.

App now holds a disjoint LocalBackend config/root or ServiceBackend Client. Local IO entry points require the original local backend. Manager mode has no local config/root, original machine or local catalogue access. One ticketed Async read slot loads profiles and workflows. The existing Model/Presentation draws manager profiles, the existing workflow browser and actual server help. Unsupported local keys do nothing, and the UI explicitly labels the current workflow catalogue read-only. Ticker ownership is now an original Async, and service exit closes the client, cancels/joins its retained jobs and restores the terminal without cancelling a manager run.

tui-service-catalogue-build failed20:14:14–20:15:19Z on conflicting nullary guarded bindings. The corrected build passed20:16:33–20:17:26Z, then full model/property/golden/render/root-role tests passed N1/N8 at20:17:50–20:17:53Z. tui-service-shell-build failed20:28:26–20:29:18Z because cleanup supplied a Map rather than its values. The corrected Werror build passed20:32:43–20:35:56Z. No stale binary was used after either failure.

Actual140x36 PTY read-only manager navigation passed N1/N8 in tui-service-shell-native,20:36:35–20:37:25Z, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-service.iY1NfO3g. The strengthened tui-service-shell-native-guards passed20:44:12–20:44:55Z, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-service.YzPIbJaT. It enters profiles, selects the real workflow catalogue, reads help, ignores local launch/control shortcuts, quits normally and via SIGTERM, joins the original Popen sessions and restores terminal modes. It creates no local runner state. Both sequences also pass local TUI regressions and the existing protected HTTP/polling/SSE/quota/revocation/restart fixture. The fixture checks an empty Overview after frontend exit, and the current service event handler contains no mutation call.

Compiler-parsed import boundaries passed20:41:41–20:41:43Z with48 syntax/frontend and343 forbidden-edge fixtures. That checkpoint covered read-only frontend interaction, not the required workflow milestone. The subsequent mutation work follows below, and all application changes remain uncommitted and unreviewed in broker-source.

## TUI request and approval path

Client now re-exports the existing pure Protocol Draft, Preparation and Command observation types and a forced shared-codec decodeObservation. TUI code still imports only public Agentic.Manager.Client. Service.prepareMutation builds one pending command against the exact original Observed URI, quoted ETag and displayed value. App stores that PendingCommand before starting its one send in a distinct Async slot. Mutation states retain preparation, sending, receipt waiting and uncertainty. Reads never resend. An uncertain attempt can be sent again only through the visible x then y confirmation using the unchanged pending object.

The TUI now creates a request, sends each exact logical literal, waits for its matching input effect and fresh request observation, explicitly enqueues it for review, and presents the complete five-selector summary. Review matching uses the Runtime frontendLiteralBytes function, including the prompt LF, while preserving the operator's logical Unicode text unchanged. The full public review is available in its own viewport without the generic display truncation. Clipped selectors, expired preparation, detail view, stale observation or pending mutation disable approval. Enter never approves. The y action binds the displayed original preparation and all five selectors.

Shared request observations clear preparationId after association. App now follows the associated request without inventing a retained preparation selector. It retains approval receipt uncertainty separately from independently authorized request/run observations. A receipt-read refusal no longer discards a successful request observation. Previously validated receipt facts may remain as history, and their resource/operation/profile binding is checked before they advance input state. No receipt is relabeled as a runtime result. Exit with an unresolved command gives a fixed warning after restoring the terminal and does not cancel the manager's run.

tui-mutation-build failed21:30:21–21:30:35Z on a missing workflowInputName import. Corrected builds passed21:31:59–21:32:42Z,21:48:37–21:49:00Z and22:12:22–22:12:44Z. The first native approval sequence failed21:49:36–21:50:48Z waiting for review, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-approval.y7mBQVzY. A second sequence with at most one test-operator-confirmed exact enqueue resend failed21:57:06–21:58:13Z after actual approval had associated a run, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-approval.RpOahG5G. The final screen showed503 while reading observations. Do not infer that every intervening read failed. The associated Request artifact has preparationId:null. Source inspection established the incorrect preparation-selector requirement and the receipt-first short circuit in the new App. Those two frontend assumptions were corrected without a Store wait, budget, failure-classification or opaque-replay change. The exact server-side cause of the observed503s was not established.

tui-approval-native-independent-observation passed22:13:10–22:14:01Z at N1/N8, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-approval.BLns6Psb. The strengthened tui-approval-native-detach passed22:18:11–22:19:12Z at N1/N8, root /Users/johnw/Products/k.M0a5ItPm/tmp/tui-approval.4bm7f1sn. It uses the actual agentic-run TUI against a separate RoutingFixedPointProbe manager/default-broker/deterministic ACP profile. It types Unicode through the PTY, checks the exact server literal, sees every complete selector, verifies Enter and detail-view y do not approve, explicitly approves from the summary, observes association in the TUI, quits, and confirms public RunControl remains owned with cancellation allowed. The saved control also offers the pending answer at occurrence1/generation4. The original frontend and server processes are joined. The fixture stops the manager after checking detach. It does not complete the workflow.

Current pure tests cover shared DTO receipt meanings, prompt-LF byte/hash matching, missing/changed selectors, exact approval body, expiry, complete layout and different-resource effect refusal. Full local model/property/golden/render/root-role checks passed at N1/N8 in the native sequences. tui-mutation-boundaries passed22:20:25–22:20:27Z with48 syntax/frontend and343 forbidden-edge fixtures. Earlier failed roots remain preserved.

Next, add complete public RunSnapshot, RunControl and Decision observation adapters, then reuse the existing person/recovery presentation for typed false and the offered retry. Retrieve exact bytes through Client.downloadVerified and complete one uninterrupted real PTY journey. Complete Overview/event attachment, stale/uncertain negative checks, residual general-CA validation and the service review's liveness obligations remain open. No new WM/G milestone is accepted.

Refocus checked2026-09-23T04:14:25Z,55minutes53seconds after the03:18:32Z deadline. That deadline was missed. Too much time went into speculative page/cursor and ownership analysis before consulting existing code. The existing Commands owner already enforces credential mutation rates, so no new rate subsystem is needed.

Refocus resumed at2026-09-23T22:31:58Z, with next deadline23:31:58Z. The first actual TUI workflow remains the active goal. Canonical is clean at tracking-onlya8bf8c115e2d01701ba49699f72e0687a3a257cc, with application/dependency baseline70bf071075a8c76357c8bb35510748aa2490fbf7. WM023 comment306 records the submission/approval evidence, with231issues,325dependencies,149labels and306comments. The isolated TUI genuinely submits Unicode and approves its exact review. Current work is complete public run/decision/control observation, typed false, offered retry and Client.downloadVerified result retrieval through that same frontend. Shared Runtime snapshot structures will be display observations only, with no fabricated Envelope, question/result path, launch capability or control authority. No duplicate interpreter or independent UI is being introduced. All application changes remain unreviewed WIP, and no new milestone is accepted. Preserve receipt uncertainty, original handles, settled Store policy and every prior failure.

The later full workflow retest tls-owning-native remained queued until its preparation deadline and is still a failure. Do not replay that request or infer a cause. Residual upstream Name Constraints coverage is acat-tls-name-forms-6gbo. All application WIP and prior failures remain preserved, with no new WM/G acceptance. Continue the actual frontend path and its concrete safety obligations without a general cryptography rewrite or another verification-machinery detour.

Every shell enters canonical direnv and the existing private environment, with one serialized credentials.QHYL3FHM/build, offline Bash Cabal wrapper, umask077, bytecode suppression and initial GHCRTS/AGENT_CAT_* absence checks. The user now authorizes crypton-x509 and crypton-x509-validation1.9.1 through Nix, with compatibility-required changes within that targeted update. No paid backend, live infrastructure/credential mutation, external-host execution, unrelated dependency/compiler change or new Lean/oracle build is authorized.
