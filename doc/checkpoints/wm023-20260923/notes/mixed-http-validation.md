# Protected mixed-workflow continuation

This record describes isolated, unreviewed service work on the broker base678326b. No new WM package or gate is accepted. The TUI service path is not implemented. All commands ran locally through canonical direnv and the existing private environment, with one serialized build directory. Each prefix below retains its command, start, end, exit and log files beside this record. Original roots remain intact.

## Current result

`http-mixed-ordered-loans` passed the complete sequence at N1 and then N8 on2026-09-23T09:17:56–09:19:14Z, exit0. Both runs executed the real `mixed-controls` program through foreground HTTPS, original manager and Runtime owners, the default broker, and the deterministic ACP retry adapter. They submitted Unicode literal input, obtained and explicitly approved all five exact selectors, answered a real flag question with JSON false, invoked offered recovery retry, observed native success, retrieved exact verified source-result bytes, and observed cleanup-governed request admission release. Polling/SSE, quotas, live revocation, original process joins and same-root restart also passed.

The current complete root is `/Users/johnw/Products/k.M0a5ItPm/tmp/mixed-http.n7QrnvFN`, with separate N1 and N8 trees. This is actual HTTP execution, not yet actual TUI interaction, and not an unconditional liveness guarantee under contention.

Earlier `http-mixed-draft-loan` passed the complete N1 portion and failed N8 before preparation publication. The public request returned to draft with admission released and no run ID. Read-only inspection found no admission observations or public preparations and a released reservation with pending_kind worker-lost. Its exact cause remains unproved. Later success does not identify that cause or retroactively prove its cleanup. The original root `/Users/johnw/Products/k.M0a5ItPm/tmp/mixed-http.ELFcO6Wl` remains intact.

## Recorded attempts

All times are UTC on2026-09-23. Product roots in this table are beneath `/Users/johnw/Products/k.M0a5ItPm/tmp/`.

| Prefix | Start–end | Exit | Root and disposition |
|---|---|---:|---|
| http-mixed-first |07:17:40–07:18:35|1|mixed-http.MaZyJ6HP/N1. Enqueue was effected but the request remained queued without a reservation. The scheduler had consumed a wake-up on an unentered configuration callback. |
| http-mixed-admission-fixed |07:31:43–07:33:02|1|mixed-http.yNU3MmKI/N1/http. Deferral checks passed. Enqueue returned503 after durable intent and reservation creation. No response success was fabricated. |
| http-mixed-reconcile |07:39:43–07:40:29|1|mixed-http.qi6iaulR/N1/http. The operator fixture explicitly resent the retained exact attempt once. Preparation later returned to a released draft. |
| http-mixed-assembly-diagnostic |07:49:47–07:50:48|1|mixed-diagnostic.rGZ1AlnN. Fixed-stage instrumentation showed accepted assembly acquired files and read the request, then failed before its catalogue lookup completed. |
| http-mixed-assembly-fixed |07:53:59–07:55:13|1|mixed-http.RWg99vXd/N1/http. Preparation still returned to a released draft. This did not prove the later failure was the same one. |
| http-mixed-stage-diagnostic |07:59:03–07:59:40|1|mixed-diagnostic.2Uu3eB04. Assembly succeeded and Worker reached capability discovery. WorkerClosed alone did not identify the initial cause because fixture teardown also closes workers. |
| http-mixed-drive-diagnostic |08:07:44–08:08:52|1|mixed-diagnostic.okJXpOK1. The one confirmed exact enqueue resend also returned503. Assembly and service failure markers were retained. |
| http-mixed-drive-diagnostic-second |08:13:22–08:13:27|1|mixed-diagnostic.OZ4idrwo. Native preparation reached Service, then review projection returned InvalidInput. The fixture had put the value1 in protected operator environment. |
| http-mixed-private-env-fixed |08:15:44–08:16:04|1|mixed-http.H8umH52a/N1/http. Python -B replaced that environment binding. A different pre-preparation worker-lost failure remained. |
| http-mixed-startup-fixed |08:22:22–08:22:34|1|Werror rejected an action binding that shadowed the outer Worker callback. No native attempt ran. |
| http-mixed-startup-second |08:23:23–08:24:02|1|mixed-http.UL1EiZu5/N1/http. New guard classification checks passed. Preparation still failed in this attempt. |
| http-mixed-review-diagnostic |08:26:52–08:27:11|1|mixed-diagnostic.US7x8YnW. A real public review was obtained. The harness incorrectly expected41 raw logical bytes instead of42 native prompt-transport bytes. |
| http-mixed-review-fixed |08:29:29–08:30:32|1|mixed-http.Do6t9AZf/N1/http. The run actually started and14 envelopes were ingested. The harness incorrectly waited for approval receipt effect-observed. |
| http-mixed-observe-runtime |08:35:09–08:35:44|1|mixed-http.0kbTXIr4/N1/http. Actual RunSnapshot failed the frozen schema. Native recovery options omitted required nullable public targets. |
| http-mixed-recovery-projection |08:39:39–08:40:38|1|mixed-http.RRCUEhtX/N1/http. The actual HTTP workflow completed and verified bytes were downloaded. A subsequent SSE registration was refused, so the whole wrapper failed. |
| http-mixed-complete-first |08:43:53–08:43:57|1|mixed-http.OruuN2gY/N1/http. The first authorized ProfilePage request was not200. Its exact status was not recorded, so do not retroactively assert it was503. |
| http-mixed-complete-second |08:45:54–08:45:58|1|mixed-http.jjBbGGc2/N1/http. A preparation returned to a released draft. |
| http-mixed-draft-loan |08:52:13–08:53:16|1|mixed-http.ELFcO6Wl. Complete N1 passed. N8 failed before public preparation as described above. |
| client-bound-observation-build |09:04:03–09:04:06|0|Public Client additions compiled with Werror. They have not executed natively. |
| response-order-first |09:15:09–09:15:31|1|Compilation stopped because two list concatenation operators were omitted while replacing Overview lines. No native check ran. |
| response-order-second |09:16:29–09:16:59|0|response-order.7wBSLZTu. N1/N8 order barriers, callback refusal, retained original file/configuration loans and escaped materializer checks passed. |
| http-mixed-ordered-loans |09:17:56–09:19:14|0|mixed-http.n7QrnvFN. Complete N1 then N8 real HTTP mixed workflow and transport sequence passed. |

## Changes and evidence limits

`tryConfigurationCatalogueContext`, `tryWithStoreCatalogues` and `tryWithStoreFiles` expose a distinct unentered outcome. Nothing means the original guard was not acquired and the callback did not run. Entered failures stay distinct. Existing entry points preserve their fail-fast refusal behavior. Native checks cover both distinctions and absence of publication on deferral.

`Admission.pollAdmission` shares the original selection/launch path with `admitOldest`. Service keeps a coalesced wake-up pending only for AdmissionDeferred. Opaque admission failures are still recorded without timer replay. Accepted assembly postpones only unentered file/catalogue callbacks within its existing five-second budget. Worker startup similarly postpones unentered configuration/file callbacks within its original thirty-second preparation deadline and stop race. No SQL admission mode was changed, and entered operations are not replayed. Independent review confirmed these distinctions and unchanged deadlines, while blocking the then-current response composition for a separate order violation.

`Drafts.withDraft` now acquires the original file loan before the authorization/configuration loan and uses a private readDraftAt helper. This corrects the reverse order in that endpoint. Other composed observation paths still need review.

Temporary traces were diagnostic only. They printed fixed stage labels or fixed failure constructors, not private exception text or input data. All temporary Debug.Trace changes in Drafts, Worker, Service and Approval have been undone. An empty native directory after cleanup was not proof that a process never launched. Later diagnostic evidence showed real preparation could have been produced and then discarded.

The fixture's protected environment value1 caused the exact-review privacy refusal. The fixture now supplies only its private adapter PATH and uses Python -B. The privacy guard was not weakened.

Logical literal text remains unchanged. `frontendLiteralBytes DescriptorPrompt` appends the declared LF for native prompt transport. Approval input length/hash checks now use those native bytes. The TUI proposal's raw-literal hash suggestion must be corrected when implemented.

Canonical tests explicitly require the original approval receipt to remain dispatch-attempted even after terminal Runtime evidence. The HTTP fixture now follows the independently observed run without falsely completing that receipt, and verifies the distinction after downloading the result. The TUI must retain that outstanding delivery state rather than block all later controls waiting for a nonexistent approval effect.

`State.publicRecoveryOptionValue` now emits both choice and required nullable target. State control/decision DTOs and Observation snapshots use that public adapter. Native wire serialization is unchanged.

The `mixed-confirm` test scenario explicitly chooses at most one identical resend after an uncertain503 response. URI, key, body and precondition remain unchanged. This is a scripted operator choice, not permission for production Client to retry mutations. Fresh bounded GET observations and refused subscription admissions may be resampled. Terminal runtime state, receipt state, cleanup, durable release and result verification remain distinct assertions.

## Independent review and next work

The read-only service-liveness review is workflow `28b08d83-a725-4ddd-938d-8a9e061cbe85`, child `a88e6ad4-c916-4c04-9f33-ef5ef06e9f86`, key `service-liveness-review`. The parent supplied `service-liveness-candidate.patch`,304613 bytes. Client is outside this review and may change independently. The reviewer must not run commands/tests/Git, edit source, delegate, inspect live settings or credentials, or traverse fixture stores.

The final report `service-liveness-review.md` issued BLOCK for reverse file/configuration loan order in Overview readers, Service.withRun and State.withDecision. It confirmed that the guard-result postponements preserve original SQL policy and deadlines, and distinguished legitimate entered fail-fast refusals from composition bugs. It explicitly rejected automatic re-enqueue or weakening the native positive assertions into eventual success. The exact earlier N8 cause remains unproved.

Parent corrected the three response owners to acquire one original file loan before the authorization/configuration loan. Draft, managed-run and decision materializers borrow that retained root. Standalone question verification retains its existing loan-acquiring entry point for answer validation. The existing test owner gained barriers that exhaust readers while holding files, requiring StoreBusy before reader/configuration admission rather than StoreLimit, then checking callback non-entry and release. Positive Overview callbacks retain both original loans, revalidate successfully, and escaped materializers refuse. These checks passed N1/N8, followed by the complete real HTTP sequence above. Continued independent clearance has not yet been requested.

The separate source-proved slow-response ceiling remains open: Service ingestion retries StoreBusy for five seconds, while a response can retain configuration for multiple five-second writes. No held-response liveness check has yet resolved that policy limit. Do not claim that order correction fixes it, drop response authority, widen waits, make locks reentrant or retry opaque operations. The review's other focused negative checks remain pending.

Subsequent Client work adds explicit client-profile loading, verified TLS configuration, strict capabilities and bounded complete page assembly. The original twelve-case boundary suite passed, then actual-manager and positive IP-SAN checks failed. The user subsequently authorized the certificate-library update. The1.9.1 dependency family plus the shared SAN-dispatch correction now passes eighteen client cases and actual protected-manager observation at N1/N8. Canonical bounded integration is70bf071075a8c76357c8bb35510748aa2490fbf7. No client trust bypass was added, and residual general-CA validation concerns remain acat-tls-name-forms-6gbo.

The updated-environment full workflow retest tls-owning-native failed19:20:34–19:21:42Z at N1. Its request remained queued with waiting admission and no preparation or run until the observation deadline. Its stderr was empty, the cause is not proved, and the request was not replayed. Root /Users/johnw/Products/k.M0a5ItPm/tmp/tls-owning.k0E71nvQ remains preserved. This does not supersede the earlier successful HTTP evidence or prove current full-workflow completion. TUI integration and its real PTY journey remain unimplemented. See tls-update.md for exact scopes and failures.
