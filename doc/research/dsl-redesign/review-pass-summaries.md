=== deep-review (clean=False, aborted=False)
I read the whole DSL subsystem line by line: `Agentic/Core/Dsl/{Syntax,Parse,Check}.lean`, `Agentic/Core/Dsl.lean`, `Agentic/Core/DslFlagship.lean`, `Agentic/Core/Explain.lean` (DSL-facing parts), `cli/AgentCat.lean` (loader/override path), `Agentic/Core/Mcp.lean` (untrusted-source entry), `test/DslSmoke.lean`, the three `.wf` examples, `example/HardenPatch.lean`, `Agentic/Surface.lean`, `Agentic/Core/Plan.lean` (`revising`, `panel`), `Agentic/Core/Question.lean` (`Shape`, `atModel`) and `doc/dsl-guide.html`, plus HANDOFF §3.6/§4.6. No builds were run; every claim below is from reading, with the reduction/fuel arguments done by hand. The fuel discipline is sound — I verified the invariants `fuel ≥ |remaining|` for `lexAux`, `scanString`, `parseBlockFrom`, `parsePanelMembers` and `parseDefines`, so the "internal: budget exhausted" branches really are unreachable and DslFlagship's "not proved #3" is an honest statement. The type-level soundness claim is also real, and `parseAndCheck_level_le` / `checkBlock_bounded` are genuine, load-bearing theorems. The serious problems are not in the fuel or the level algebra: they are in `define`, which is a textual macro expanded before any scope exists and which silently captures `let`/`given` binder names and silently changes a question's rung; in error positions and diagnostic ordering; in an eleven-field positional `revising` constructor; and — the owner's complaint, which I found is mechanically demonstrable rather than a matter of taste — in a grammar where `{ }` carries four different meanings and where the `revising` statement's four clauses straddle two brace depths with nothing lexically joining them.

=== alexey (clean=False, aborted=False)
I read the DSL subsystem end to end (Syntax/Parse/Check/Dsl/DslFlagship/Explain), hand-traced `example/harden.wf` through the lexer, `define` expansion, the grammar and the checker, and confirmed by hand that every `Pos` and every `Chunk` list in `Dsl.flagshipRaw` matches the file byte for byte, that the elaborated plan has `size = 36` and `askNodes = 19`, and that its cost tree has exactly the leaves {5,6,7,9,10,11,13,14,15} — so the guide's numbers, `card_leaves_flagship`, `minFold_flagship`, `maxFold_flagship` and the four bill theorems are all mutually consistent and correct. The elaboration is faithful to the source and `checkBlock_level_le` genuinely establishes the one property the language exists for. The defects are elsewhere. On the premise: nothing in the level theorem requires textuality — `Agentic/Core/Dsl.lean` already proves the per-former bounds over `Plan`, so the same guarantee is available from a restricted Lean combinator API, while textuality costs the project its central proof (the parser is provably outside the proof boundary; `parse flagshipSource = .ok flagshipRaw` is a runtime test, and `flagshipRaw` had to be transcribed by hand). Worse, the "Lean twin" the grammar was scored against (`example/HardenPatch.lean`) is a *different workflow* on a carrier the project has itself condemned. On truth of prose: the load-bearing claim in HANDOFF §4.6 ("the new text is *proved* to elaborate to the plan the old text did") is not proved and cannot be, given how `flagshipPlan` and `check_flagshipRaw` are defined; `Check.lean`'s docstring is materially wrong about which binders are context entries; "nineteen `decide +kernel` proofs" appears in seven places and there are nine. On tests: a permutation of the three `caseVerdict` arms passes every test and every theorem in the package. On scope: `Verdict.render`/`objections` now exists in three places, which is exactly the library gap house rule 7 names.

=== abstraction (clean=False, aborted=False)
I read the doctrine kernel, then the whole DSL stratum (Syntax/Parse/Check, Dsl.lean, DslFlagship headers, Explain), the abstractions it is supposed to extend (Plan's five formers, Denote's fold and `Plan.Denotes`, the Level fold, `Q.Shape`×words), the comparison target `example/HardenPatch.lean` with `Agentic/Surface.lean`, and the grammar debate in `doc/research/dsl-chosen.md` and `doc/HANDOFF.md` §3.6/§4.6. Method: establish the null diff (a surface over five formers should be a projection of those formers plus a name-resolution table), inventory every divergence, state and attack the premise behind each, and classify extension vs evasion. Verdict: ALIGNED WITH FINDINGS, with one structural evasion at the centre. The elaborator is genuinely well aligned — `using model` as a shape rewrite licensed by `under_ask1`, `Prompt.closed` letting the syntax decide the rung, `codeOfName_codeName` as a proved retraction, `maxRevisions` refused through the ordinary `CheckError` channel and generalised by `checkBlock_bounded`, and `checkBlock_level_le` are all vocabulary extensions done right — but the *surface* is not derived from the denotation at all: the DSL stratum is the only one in the repository with no meaning function, so `Plan.case` acquired three unrelated spellings (one of which, `approved given p / never approved`, hides that it is a two-arm case), one derived idiom (`Plan.revising`) was frozen into an eleven-field grammar production that no term can hold, and `act` became a second path to a node the language already writes. The owner's complaint reads, in these terms, as the exact symptom the missing morphism equation predicts.

=== validated (clean=False, aborted=True)
Scope examined: the frozen snapshot's DSL subsystem — Agentic/Core/Dsl/Syntax.lean, Agentic/Core/Dsl/Parse.lean, Agentic/Core/Dsl/Check.lean, Agentic/Core/Dsl.lean, example/harden.wf, example/hello.wf, example/ill-typed.wf, example/HardenPatch.lean and doc/dsl-guide.html, plus corroborating reads of Agentic/Core/Plan.lean, Agentic/Surface.lean, Agentic/Core/HardenPatch.lean, Agentic/Core/DslFlagship.lean and cli/AgentCat.lean. Method: read the primary files myself, then obtained one independent adversarial review from GPT-5 (via mcp__pal__clink cli_name="codex", which attested itself as GPT-5), then checked each of its claims against the snapshot text with Read and grep. Read-only throughout; no git, no lake/lean/direnv, no writes outside my namespace. Verdict on the protocol: ABORTED — the OpenAI and Gemini API keys configured for the PAL server are invalid (unexpanded `${OPENAI*****KEY}` and an invalid Gemini key), the gemini CLI is absent, so only one non-Anthropic model existed and cross-model verification was impossible; per contract I did not stand in for the second model. Verdict on the code: not clean. The strongest provisional findings are that `define` expansion is unhygienic and can silently shadow a `let` binding, turning an open prompt into a closed one and demoting a `Plan.ask` (pipeline) to `Plan.askC` (batch); that eager `define`-on-`define` expansion is exponential at parse time with no budget guarding it; that a panel containing a `declined` verdict annihilates every objection so the `why` handed to the next revision is the empty string; and that the owner's readability critique of the `revising` surface is substantiated by concrete evidence (three distinct binders all spelled `patch`, purely positional roles in `revise given p, why`, mandatory outcome clauses written outside the loop's own braces, and a parser that must special-case `revising` in `parseRhs` to explain to the reader why it cannot be bound). Two GPT-5 claims I refuted from the source and therefore did NOT report as findings: (a) it claimed Check.lean:536-541 documents the wrong return type — false, that sentence is a hypothetical about a hypothetical checker, not a claim about `Dsl.check`; (b) it graded the exhausted-path divergence P0 and attributed it to the DSL — the DSL faithfully mirrors the Plan flagship (Agentic/Core/HardenPatch.lean:753 documents the same give-up behaviour), so the divergence is between the Plan flagship and the owner's do-block surface and is reported below at reduced severity with corrected attribution.

=== ponytail (clean=False, aborted=False)
Read the whole DSL subsystem (Syntax.lean 274 lines, Parse.lean 555, Check.lean 575, Dsl.lean 369, Explain.lean 527, the three .wf examples, plus every call site in cli/AgentCat.lean, Agentic/Core/Mcp.lean, test/DslSmoke.lean and test/CliSmoke.lean) hunting only for complexity to delete. Method: grep every exported name in the subsystem for a real consumer, and compare each construct against what the three example programs and the smoke tests actually exercise. On the brief's explicit question — the parser's hand-rolling is *justified*: `Std`/`Lean.Parsec` and `String.toNat?` all iterate with well-founded recursion, which does not reduce in the kernel, and `DslFlagship.lean` proves by `decide +kernel`; so the fuel-based lexer stays. What does not survive is elsewhere: a ~105-line `bounded`/`checkBlock_bounded`/`check_bounded`/`revisionBounds_le_of_bounded` proof chain that no code and no test consumes, two `rfl` theorems that restate a definition made on the line above, three copies of one verdict renderer with a theorem asserting two of them are one function, a `Prompt.normalize` pass that is provably the identity on the parser path, and a duplicated `checkAskAt`/`checkMembers` pair whose type parameter has exactly one call-site value. About 230 lines can go without changing a single accepted or rejected program, plus one brace level out of the construct the owner called confusing.

=== dead-code (clean=False, aborted=False)
I read the whole DSL scope (Dsl/Syntax.lean, Dsl/Parse.lean, Dsl/Check.lean, Dsl.lean, DslFlagship.lean, Explain.lean), the three .wf examples, doc/dsl-guide.html, doc/mcp.md, and — for the superseded-stratum question — lakefile.toml, Agentic.lean, README.md, flake.nix, HANDOFF.md and every import edge in the repository. Method: for each candidate I required two independent signals (a whole-snapshot grep showing no consumer AND a structural reason it cannot be reached / a contradicting current artifact), and I discarded everything that survived on only one. Verdict on the specific items the brief named: no `Raw` constructor is dead (the parser produces all six), `CheckError` is a structure so there are no unconstructed variants, no `Parse.lean` helper is callerless, and the two `Explain` branches that look unreachable (`Plan.explain`'s `.dyn` clause, `costLines`'s `else`) are genuinely exercised by `test/CliSmoke.lean:237-245` and are correctly documented — all clean. The superseded `Agentic/*.lean` stratum does **not** leak into the DSL modules (Dsl/* reaches only Plan → Dlg → World → Question → Scope → Monoid, all live), but it is kept alive by `example/HardenPatch.lean`, which is exactly the artifact the review brief names as the DSL's comparison target. What I did find is one superseded theorem with no consumer, two provably unreachable checker clauses, one unused Explain lemma, and a cluster of documentation — README.md most seriously — that describes a repository state two re-derivations old.

=== comments (clean=False, aborted=False)
I read every module docstring, declaration docstring and inline comment in the DSL scope (Agentic/Core/Dsl/Syntax.lean, Parse.lean, Check.lean, Agentic/Core/Dsl.lean, Agentic/Core/DslFlagship.lean, Agentic/Core/Explain.lean), the three example/*.wf headers, and every code block, grammar snippet, message quotation and numeric claim in doc/dsl-guide.html, and checked each against the current code by reading only — no lake/lean/direnv was run and no file was modified. Grammar conformance of the guide was verified against Parse.lean clause by clause: all three `ask` forms, `let`, `panel`, the full `revising` shape, `if`/`case`, `act`, `{ }`, the escape set, the punctuation set, the comment syntax and the whole §8 reference card match the parser exactly; the §3 table of refusal messages matches the strings in Parse.lean/Check.lean verbatim; the eight-construct/thirty-eight-lexeme count checks out (32 words + 6 punctuation); example/harden.wf's every position matches flagshipRaw's `Pos` fields; the guide's `binds #2`/`{#0}`/`{#1}` sample is consistent with Explain.Env.probe's absolute numbering; and the `agent-cat run` sample lines match cli/AgentCat.lean and Report.lean verbatim. Eight claims are refuted by the code and are reported below; the most consequential are a proof-count repeated in three files that is off by a factor of two, a documented invariant about the parser's fuel branches that one branch violates, and a Check.lean docstring that misdescribes which binders are context entries. All measurement claims (kernel-reduction timings, memory, elaboration minutes) are unverifiable statically and are recorded as observations rather than defects.

