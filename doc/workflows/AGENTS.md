# Manual workflow maintenance

- This directory is documentation tooling, not the Haskell `workflow/` module.
- Preserve `agent-cat-manual.js` bytes unless rerunning and updating its smoke record.
- Fix generated Texinfo only after the normal source/API/prose/manual gates pass.
- Do not encode providers, credentials, machine paths, or agent-cat execution policy.
