# Manual workflow maintenance

This directory is documentation tooling, and it is unrelated to the Haskell
`workflow/` directory. Preserve the bytes of `agent-cat-manual.js` unless the
smoke record is rerun and updated with it. Accept generated Texinfo only after
the source, coverage, prose, and manual gates pass. Encode no provider,
credential, machine path, or agent-cat execution policy here.
